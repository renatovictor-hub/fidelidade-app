{
  "name": "fidelidade-app",
  "version": "1.0.0",
  "description": "Fidelidade Restaurant App",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build needed'",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "@vercel/analytics": "^1.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
